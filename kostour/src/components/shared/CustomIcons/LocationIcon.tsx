import { ShieldCheckIcon } from "@heroicons/react/solid";
import React from "react";

type Props = {};

const LocationIcon = (props: Props) => {
  return <ShieldCheckIcon className="w-[40px] text-blue-600" />;
};

export default LocationIcon;
